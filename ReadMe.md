# KIRO AI DEMO

### How did I build it

#### Backend

1. set up Express App on `backend/index.js`

#### Frontend
1. on `frontend/src/App.tsx` set up Type, States, Submission function
  - created 4 states: errorMessage, results, loading and error
  - create a form with `<textarea>`and a submit `<button>` and apply submission states
  - under the form then hide and error warning with div
  - if it doesn't have any errors then we print results


